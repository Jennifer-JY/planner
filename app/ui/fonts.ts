import { Caveat_Brush, Merriweather, Roboto } from "next/font/google";

export const roboto = Roboto({
  subsets: ["latin"],
  weight: ["400", "700"],
});

export const caveateBrush = Caveat_Brush({
  subsets: ["latin"],
  weight: ["400"],
});

export const merriweather = Merriweather({
  subsets: ["latin"],
  weight: ["400"],
});
